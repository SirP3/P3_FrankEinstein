# v0.1 Donor Map

## Purpose

This file is the public-safe anatomical map of the old P3FE v0.1 archive.

It does not import old content.

It only describes which reusable system organs exist in the donor archive.

## Donor role

`P3_FrankEinstein v0.1_archive` is a donor/archive workspace.

It is not the active development workspace.

Nothing is copied from the donor into v0.2 without audit.

## Useful donor organs

### YouTube Mining module

The old archive contains a working YouTube Mining module.

Known public-safe components to inspect later:

- YouTube Mining module folder
- Streamlit UI application
- handoff generation logic
- YTM runbook
- YTM specs
- YTM audit notes
- local stack notes

### Local stack notes

The old archive contains local tool/setup notes.

These may help reconstruct:

- local AI model usage
- UI launch workflow
- module startup commands
- local operator workflow

### Safety material

The old archive contains safety-related notes and scripts.

These should be reviewed and selectively rebuilt in v0.2 instead of copied blindly.

## Blocked donor zones

The following old archive material must stay local-only unless explicitly anonymized and approved:

- raw transcripts
- VTT/TXT files
- source-derived handoff packs
- radar-card outputs
- backup code dumps
- private config
- local-only notes
- business-specific context
- personal or company-specific identifiers

## Rebuild rule

Do not clone the old body.

Extract the organs, describe their function, then rebuild consciously.

## Next use

This donor map supports the next phase:

1. inspect YTM v1.1 working state
2. write YTM v1.1 blueprint
3. define YTM v1.2 target
4. import only audited code
